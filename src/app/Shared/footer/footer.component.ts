import { Component } from '@angular/core';

@Component({
  selector: 'app-footer',
  standalone: true,
  imports: [],
  templateUrl: './footer.component.html',
  styleUrl: './footer.component.css' // Corrige 'styleUrl' a 'styleUrls'
})
export class FooterComponent {
  // Aquí se pueden agregar propiedades o métodos relacionados con el footer
}
